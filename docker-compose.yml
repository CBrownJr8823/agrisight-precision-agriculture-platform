services:
  agrisight:
    build: .
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./data/agrisight.db
    volumes:
      - agrisight_data:/app/data
    restart: unless-stopped

volumes:
  agrisight_data:
